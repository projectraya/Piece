[phases.setup]
nixPkgs = ['dotnet-sdk_8']
nixpacksVersion = '1.38.0'

[phases.install]
cmds = ['dotnet restore']

[phases.build]
cmds = ['dotnet publish -c Release -o out']

[start]
cmd = 'cd out && dotnet Piece.dll'
