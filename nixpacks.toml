[phases.setup]
nixPkgs = ['dotnet-sdk_8']
aptPkgs = []

[phases.install]
cmds = ['dotnet restore Piece/Piece.sln']

[phases.build]
cmds = ['dotnet publish Piece/Piece/Piece.csproj -c Release -o out']

[start]
cmd = 'dotnet out/Piece.dll'
